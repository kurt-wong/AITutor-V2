# AI Agents 指南

## 智能代理路由

请根据任务类型自动选择最合适的代理：

### 使用 DeepSeek V4 Pro 的场景
当任务涉及以下内容时，自动切换到对应代理：
- **架构设计** → 使用 `architect` 代理
  - 系统架构设计
  - 技术方案制定
  - 架构评审
  - 技术选型决策
  
- **复杂调试** → 使用 `debugger` 代理
  - 复杂Bug根因分析
  - 深度调试
  - 性能问题排查
  - 异常行为追踪
  
- **大规模重构** → 使用 `refactoring` 代理
  - 跨模块重构
  - 代码结构优化
  - 架构升级
  - 大规模代码清理
  
- **代码审查** → 使用 `code-review` 代理
  - PR代码审查
  - 质量评估
  - 规范检查
  - 安全审计
  
- **最终架构评审** → 使用 `architecture-review` 代理
  - 架构决策评审
  - 技术方案评估
  - 架构优化建议

### 使用 MIMO 的场景
当任务涉及以下内容时，自动切换到对应代理：
- **普通代码实现** → 使用 `coder` 代理
  - 功能开发
  - 代码编写
  - 模块实现
  - API开发
  
- **简单修改** → 使用 `simple-fix` 代理
  - 小Bug修复
  - 配置调整
  - 文档更新
  - 简单优化
  
- **测试生成** → 使用 `test-generator` 代理
  - 单元测试生成
  - 集成测试编写
  - 测试用例设计
  - 测试覆盖率提升

## 代理切换规则

1. **自动识别**：根据用户请求的内容自动判断任务类型
2. **主动切换**：在回复前先切换到合适的代理
3. **明确说明**：切换代理时告知用户当前使用的代理和原因
4. **保持上下文**：切换代理时保持对话上下文的连续性

## 使用示例

- "帮我设计一个用户认证系统" → 切换到 `architect` 代理
- "这个Bug很难找，帮我分析一下" → 切换到 `debugger` 代理
- "帮我重构这个模块" → 切换到 `refactoring` 代理
- "帮我写一个登录功能" → 切换到 `coder` 代理
- "帮我修复这个样式问题" → 切换到 `simple-fix` 代理
- "帮我写单元测试" → 切换到 `test-generator` 代理

## 代理协作模式

### 串行协作
当任务需要多个代理协作时，可以按顺序切换代理：
1. `architect` 设计架构
2. `coder` 实现代码
3. `code-review` 审查代码
4. `test-generator` 生成测试

### 并行协作
对于复杂任务，可以同时使用多个代理：
- `architect` 设计架构，同时 `coder` 准备开发环境
- `debugger` 分析Bug，同时 `simple-fix` 准备修复方案

## 代理选择优先级

当任务涉及多个方面时，按以下优先级选择代理：
1. 架构设计和评审 → `architect` 或 `architecture-review`
2. 复杂调试和分析 → `debugger`
3. 大规模重构 → `refactoring`
4. 代码审查 → `code-review`
5. 代码实现 → `coder`
6. 简单修改 → `simple-fix`
7. 测试生成 → `test-generator`

## 注意事项

1. **上下文保持**：切换代理时，确保关键上下文信息被传递
2. **任务完整性**：确保每个代理完成其职责后再切换
3. **质量把关**：重要任务完成后，建议使用 `code-review` 进行审查
4. **文档记录**：复杂决策和架构设计应记录在文档中
