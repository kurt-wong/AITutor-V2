# 语文 Q17 人工审核材料（2026-08-25）

> 审核对象：朝阳高一（上）期末语文（教师版）Q17（默写题）。
> 结论先行：**串题为真实数据问题（L2 标注行号跨区），答案无真实冲突（假冲突）**。

## 1. 串题（真实，需要决策修复）

**现象**：DB Q17 stem = 1840 字符 = 默写 prompt（正确部分 ~300 字）+ 下一 section
（散文阅读《到泗洪去》）标题与材料整段（~1500 字）。

**根因链**（已逐层确认）：

1. L2 标注给 Q17 的 `stem_line_ids` 共 19 行 = `P5L014 ~ P6L006`：
   - `P5L014-P5L021`：17. 默写 prompt（正确）
   - `P5L022`：OCR 噪声行 `0`
   - `P5L023`：`四、本大题共4小题，共18分。`（**下一 section 标题**）
   - `P5L024`：`阅读下面作品，完成下面小题。`
   - `P5L025-P6L006`：`《到泗洪去》` 材料（**Q18-21 的共享材料行**，
     被 L2 同时标给 Q17 的 stem 与 Q18 的 shared_material_line_ids）
2. content_slicer 忠实切片 19 行 → Q17 stem 1840 字符。
3. 膨胀门（上限 800）拦截 → status=reviewing（**保护生效**，未自动发布坏数据）。
4. e2e 越界检查确认：`stem 越界/串题: section:散文阅读_1, 四、本大题`。

**影响**：Q17 当前不可独立使用（题干含无关材料）；Q18-21 材料行被重复归属但内容
未被破坏（Q18-21 均 approved，材料覆盖 100%）。

**修复选项**：

- **A（推荐，DB 数据修复，快）**：将 DB Q17 stem 截断为仅默写 prompt
  （截至 `P5L021` 行），答案与 PDF 答案区一致（见下），直接转 approved。
  不动其他题、不重跑管线；代价是 DB 文本与 L1 行号不再严格对应（DB 无行号约束）。
- **B（重跑语文文档）**：LLM 重标注期望修正 Q17 行号范围。08-23/08-25 两次重灌
  均产生同样错误，LLM 随机性下不一定修好；耗时 + LLM/OCR 成本。
- **C（接受现状）**：Q17 保持 reviewing，标记为已知问题，不推荐该题出题/作答。

## 2. 答案（无真实冲突，假冲突）

**DB 答案**（两次重灌一致）：`①. 何时可掇 ②. 别时茫茫江浸月 ③. 则天地曾不能以一瞬
④. 而又何羡乎 ⑤. 人生如梦 ⑥. 一尊还酹江月 ⑦. 绛皓驳色 ⑧. 而皆若偻`

**PDF 答案区原文**：`17.【答案】①.何时可掇②.别时茫茫江浸月③.则天地曾不能以一瞬
④.而又何羡乎⑤.人生如梦⑥.一尊还酹江月⑦.绛皓驳色⑧.而皆若偻`

→ **完全一致**（仅空格差异）。

**answer_conflict 标记来源**：两次重灌答案仅内部空格不同（`①.何时可掇` vs
`①. 何时可掇`），`ingestion.py` 去重比较用 `.strip()` 只去首尾空白 → 假冲突。
08-25 灌入时命中 content_hash（题干+选项+题型一致）→ 比较答案不等 → 标记
`answer_conflict` 并降级 reviewing。**该标记可人工确认后清除**。

可选改进（代码侧）：去重比较前先 compact 化（去空白/全角归一）答案再比对，
避免空白差异产生假冲突——可作为独立小修复（BUG-026 候选）。

## 3. 证据索引

- 管线 Q17 stem 全长 1840；stem_line_ids 明细见本次诊断
  （tmp/diag_yuwen_q17_lines.py 输出）。
- L2 Q17 `section_id=None`、`shared_material_line_ids=[]`（根因不在共享材料，
  在 stem 行号范围）。
- v6.23 e2e 语文报告：Q17 `content_slicer` / `stem 越界/串题`。

## 4. 决策请求

- 串题修复：A / B / C？
- answer_conflict：确认答案正确后清除标记并转 approved？
  （或同时采纳 ingestion compact 比较改进，防止复发）
